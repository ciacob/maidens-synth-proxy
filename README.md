# maidens-synth-proxy

Default, shared synthesizer, used both by the principal thread and the child threads of MAIDENS' sound synthesis functionality.
The actual synthesizer engine can be substituted at compile time with another sublass of SynthProxy.
